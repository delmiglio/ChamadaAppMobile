<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="ChamadaAppMobile.MyListView">
  <TabbedPage.Children>

    <ContentPage Title="TextCell" Padding="0, 20, 0, 0">
      <ContentPage.Content>
        <StackLayout >
          <ListView ItemsSource="{Binding Items}">
            <ListView.ItemTemplate>
              <DataTemplate>
                <TextCell DetailColor="Green" Text="{Binding Nome}" Detail="{Binding Sobrenome}"></TextCell>                
              </DataTemplate>
            </ListView.ItemTemplate>
          </ListView>
        </StackLayout>
      </ContentPage.Content>
    </ContentPage>

    <ContentPage Title="SwitchCell" Padding="0, 20, 0, 0">
      <ContentPage.Content>
        <StackLayout>
          <ListView ItemsSource="{Binding Items}">
            <ListView.ItemTemplate>
              <DataTemplate>
                <SwitchCell Text="{Binding Nome}" On="{Binding Selected}"></SwitchCell>
              </DataTemplate>
            </ListView.ItemTemplate>
          </ListView>
        </StackLayout>
      </ContentPage.Content>
    </ContentPage>
    
  </TabbedPage.Children>
</TabbedPage>